import { Branch, GitControl } from "../api/git.js";
import { StableVersion } from "../core/version.js";
import { SupportPolicy } from "../core/support.js";
import { StableVersionMatcher } from "../core/matcher.js";
export declare function maintainStableVersionBranches(git: GitControl, supportPolicy: SupportPolicy, staleVersionMatcher: StableVersionMatcher): Promise<void>;
export declare function getStableVersionBranches(git: GitControl, matcher: StableVersionMatcher): Promise<StableVersionBranch[]>;
export interface StableVersionBranch {
    readonly branch: Branch;
    readonly version: StableVersion;
}
