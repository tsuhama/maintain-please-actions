import { SemanticVersion, StableVersion } from "./version";
export interface SupportPolicy {
    supports(version: StableVersion): boolean;
    getLatestVersion(): SemanticVersion;
}
export declare function createNMinusRangeSupportPolicy(latestReleaseVersion: string, majorRange: number, minorRange: number): SupportPolicy;
export declare class NMinusRangeSupportPolicy implements SupportPolicy {
    readonly latestVersion: SemanticVersion;
    readonly majorRange: number;
    readonly minorRange: number;
    constructor(latestVersion: SemanticVersion, majorRange: number, minorRange: number);
    getLatestVersion(): SemanticVersion;
    /**
     * example major: n-1, minor: n-2
     * latest version 3.2: 2.2=>true, 1.6=>false, 2.0=>true(as we don't evaluate the minor)
     * latest version 2.5: 2.0 =>false, 1.0=>true, 2.3=>true
     * @param version
     */
    supports(version: StableVersion): boolean;
    private compare;
}
